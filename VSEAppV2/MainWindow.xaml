<Window
    x:Class="VSEAppV2.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VSEAppV2"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodel="using:VSEAppV2.ViewModels"
    mc:Ignorable="d">
    <Grid>
        <Frame x:Name="mainFrame"/>
<!--
        <ProgressRing IsActive="{x:Bind ViewModel.AppState.OverallAppModel.IsLoading, Mode=OneWay}"
                      VerticalAlignment="Bottom"/>
        <TextBlock Text="{x:Bind ViewModel.AppState.OverallAppModel.LoginControl.CurrentUser.Username, Mode=OneWay}"
                   Margin="0,0,0,50"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Bottom"/> -->
    </Grid>
</Window>
